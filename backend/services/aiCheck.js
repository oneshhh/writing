async function runAiCheck(_article) {
  // Placeholder: integrate GPTZero or similar here.
  return { provider: "placeholder", score: null, status: "not_configured" };
}

module.exports = { runAiCheck };

