// Frontend runtime config.
window.APP_CONFIG = {
  // Same-origin by default (recommended). If you host the frontend separately, set this to your API origin.
  apiBaseUrl: "",
  appName: "Real Write"
};

  
